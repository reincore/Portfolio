import React from "react";
import Layout from "../components/layout";
import "./contact.scss";

const ContactPage = () => (
  <Layout>
    <h1 className="contact-header"> Contact </h1>
    <p> This is the contact page that will be implemented.</p>
  </Layout>
)

export default ContactPage;
